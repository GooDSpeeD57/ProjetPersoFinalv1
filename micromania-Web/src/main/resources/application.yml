spring:
  application:
    name: boutique-web

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8

  security:
    user:
      name: disabled
      password: disabled

server:
  port: ${SERVER_PORT:8081}

api:
  base-url: ${API_BASE_URL:http://goodspeed57.ddns.net:8080/api/v1}

app:
  remember-me:
    cookie-name: MICROMANIA_REMEMBER_ME
    validity-days: 30
